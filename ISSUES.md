* A crash may corrupts the colors (cache is kept open)
* Sometimes cells containing an Item are visible when they shouldn't be
* In some terminals the Windows are vibrating (wclear calls should be avoided)
